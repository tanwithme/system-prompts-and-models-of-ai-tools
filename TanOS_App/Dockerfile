# TanOS Application Dockerfile (Conceptual - for future containerization)
# FROM python:3.10-slim

# WORKDIR /app

# COPY requirements.txt requirements.txt
# RUN pip install --no-cache-dir -r requirements.txt

# COPY . .

# ENV TANOS_PROMPT_DIR=/app/tanos_prompts
# ENV TANOS_DATA_DIR=/app/tanos_data
# # Add other ENV VARS for API keys etc., ideally through docker-compose secrets

# CMD ["python", "-m", "tanos_core.main"]
